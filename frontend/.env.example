# משתני סביבה לפרונטאנד (Vite) — מוזרקים בזמן build.
# העתיקו ל-frontend/.env ומלאו את הערכים האמיתיים (הקובץ .env לא נכנס ל-git).

# כתובת פרויקט ה-Supabase
VITE_SUPABASE_URL=https://bpeyqirijpxpatleykzb.supabase.co

# המפתח הציבורי (anon / publishable) — מתוך Supabase: Project Settings → API
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
